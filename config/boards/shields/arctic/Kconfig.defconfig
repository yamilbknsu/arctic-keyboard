if SHIELD_ARCTIC_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Arctic"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_ARCTIC_LEFT || SHIELD_ARCTIC_RIGHT

config ZMK_SPLIT
    default y

endif